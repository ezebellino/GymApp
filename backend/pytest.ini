[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    # El ciclo de FKs users.client_id <-> clients.created_by_user_id es conocido y
    # SQLite no aplica las FKs por defecto: el drop_all del fixture funciona igual.
    ignore:Can't sort tables for DROP:sqlalchemy.exc.SAWarning
