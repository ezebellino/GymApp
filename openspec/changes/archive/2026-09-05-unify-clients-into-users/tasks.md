## 1. Pre-vuelo y red de seguridad

- [x] 1.1 Escribir `backend/scripts/preflight_unify_users.py` (solo lectura, no escribe nada): reporta colisiones `clients.email` ↔ `users.email` sin link, nombres de una sola palabra en `users.full_name`/`clients.full_name`, clientes sin email y sin teléfono, conteo de pares linkeados (`users.client_id IS NOT NULL`) y de filas de `payments`/`attendances`/`workout_logs` a reescribir, y la versión de Postgres (para validar `ALTER TYPE ... RENAME VALUE`).
- [x] 1.1b Agregar al pre-vuelo la **lista nominal (nombre + email) de `clients` con `is_active = false` que tienen cuenta de portal linkeada**: es exactamente la gente que pierde el login el día del deploy (migra a `cancelled` con rol Miembro). Tiene que verla el gimnasio antes de migrar, no enterarse por un reclamo.
- [ ] 1.2 Correr el pre-vuelo contra una copia de los datos reales y dejar el output pegado en el PR, para que las decisiones de la migración se tomen contra números y no contra suposiciones.

## 2. Modelo de datos unificado

- [x] 2.1 En `backend/app/models.py`, ampliar `User` con las columnas nuevas (dec. 2 del design): `first_name`, `last_name`, `birth_date`, `weight_kg`, `height_cm`, `phone`, `phone_verified`, `membership_status`, `membership_start_date`, `membership_cancelled_at`, `created_by_user_id` (auto-FK) y `legacy_client_id`; pasar `email` y `password_hash` a `nullable=True`; eliminar `client_id`.
- [x] 2.2 Agregar el enum `MembershipStatus` (`none | active | cancelled`) y renombrar el valor del enum `UserRole` de `user` a `member`.
- [x] 2.3 Definir `full_name` como `hybrid_property` en `User` (Python concatena; expresión SQL `first_name || ' ' || coalesce(last_name,'')` para `ilike`/`order_by`) y una propiedad `age` derivada de `birth_date` (`None` si no está cargada).
- [x] 2.4 Reemplazar la UNIQUE plana de `users.email` por un índice único parcial `WHERE email IS NOT NULL`, y agregar los índices que hoy están en `clients` y siguen haciendo falta (`full_name` expresión, `phone`, `membership_status`, `membership_start_date`).
- [x] 2.5 Cambiar `Payment.client_id`, `Attendance.client_id` y `WorkoutLog.client_id` a `user_id` (FK `users.id`, `ondelete="RESTRICT"`), actualizar las relaciones ORM (`user` en vez de `client`, sin `cascade="all, delete-orphan"`) y renombrar `uq_payment_period` a `(user_id, period_month, period_year)`.
- [x] 2.6 Eliminar la clase `Client` de `models.py`. Verificar que `TrainingDayExercise` queda **sin cambios** (no tiene `client_id`; su `assigned_by_user_id` ya apunta a `users`).

## 3. Migración Alembic (esquema + datos, una sola revisión)

- [x] 3.1 Crear la revisión con `python -m alembic revision -m "unify clients into users"` sobre la head actual (`9812b6c09a1a`). **No** usar `--autogenerate` a ciegas: el autogenerate no sabe mover datos y va a proponer un `drop_table("clients")` que perdería todo el historial.
- [x] 3.2 Paso 1-2 del upgrade: agregar todas las columnas nuevas a `users` como nullable; `email` y `password_hash` a nullable; drop del UNIQUE de email y creación del índice único parcial.
- [x] 3.3 Paso 3: backfill de las filas `users` existentes — split de `full_name` (primer token → `first_name`, resto → `last_name`, NULL si es una sola palabra), `membership_status='none'`, `phone_verified=false`, y **`email_verified=TRUE` para todo usuario con `password_hash` no nulo** (sin esto el chequeo de `auth.py:55` deja al staff fuera del sistema).
- [x] 3.4 Paso 4: insertar una fila `users` por cada `clients` **sin** cuenta linkeada, reusando `clients.id` como `users.id`, con `role='member'`, `password_hash=NULL`, `membership_start_date=join_date`, `membership_status='active'|'cancelled'` según `clients.is_active`, `legacy_client_id=clients.id`. Ante colisión de email con un `users.email` existente: insertar con `email=NULL` y loguear el caso. **Nunca auto-mergear por email.**
- [x] 3.4b A todo lo que quede en `cancelled` por el paso anterior, setearle `membership_cancelled_at = <timestamp de la migración>` (dec. 6 del design). **No dejarlo NULL**: una baja sin fecha ahora bloquea el login sin registro de cuándo. La fecha es corregible después desde el ABM, que acepta fechas retroactivas.
- [x] 3.5 Paso 5: para cada par linkeado (`users.client_id IS NOT NULL`), copiar el perfil del cliente sobre la fila `users` (phone, `membership_start_date`, `membership_status`, `membership_cancelled_at`, `legacy_client_id`) dejando email y nombre de `users` como autoritativos, **sin tocar `role`** (ya es el correcto desde antes de migrar — pisarlo con `'member'` le borraría los permisos a un Dueño/Coach que también entrena, hallazgo 8 de `verification.md`), y reescribir `payments`/`attendances`/`workout_logs` de `client_id = clients.id` a `users.id`.
- [x] 3.6 Paso 6: renombrar `client_id` → `user_id` en las tres tablas, rehacer FKs contra `users.id` con `RESTRICT`, índices y `uq_payment_period`.
- [x] 3.7 Paso 7-9: drop de `users.client_id` (FK + índice único), `drop_table("clients")` con sus índices, `ALTER TYPE userrole RENAME VALUE 'user' TO 'member'` (con el fallback de tipo nuevo + cast documentado en el design si la versión de PG no lo soporta), y `SET NOT NULL` en `first_name`, `membership_status`, `phone_verified`, `email_verified`.
- [x] 3.8 `downgrade()` levanta `NotImplementedError` con un mensaje que apunta a la estrategia de snapshot/restore del design.
- [x] 3.9 Probada de punta a punta contra un Postgres real (no SQLite) con un dataset sintético que cubre los casos borde: clientes sin cuenta, pares linkeados (activo y de baja), colisión de email contra un `users.email` existente, colisión de email entre clientes sin cuenta entre sí, nombres de una sola palabra, cliente inactivo sin portal. Verificado: conteos de pagos/asistencias preservados, ningún `user_id` huérfano, pares linkeados colapsados en una sola fila, `uq_payment_period` y las 3 FK `RESTRICT` correctas, enum renombrado a `owner/coach/member`, `downgrade()` levanta `NotImplementedError`. **No** se corrió contra una copia restaurada de datos de producción reales (no había ninguna disponible ni autorizada en este entorno) — eso queda para 13.3 junto con el resto del plan de deploy.

## 4. Schemas y lógica compartida del backend

- [x] 4.1 En `app/utils.py`, junto a `current_period()`: `membership_indicator(status, last_month, last_year, ref=None)` → `"none" | "up_to_date" | "overdue" | "suspended"` (pura) y su expresión SQL equivalente (un `CASE` sobre `membership_status` que solo para `active` compara `MAX(period_year * 12 + period_month)` contra el período actual). Aritmética entera, **sin `date_trunc`** (la suite corre sobre SQLite y no puede ejercitarlo). **Esta función no recibe ni mira el rol.**
- [x] 4.1b En `app/auth.py`, junto a `get_current_user`/`require_role`: `is_membership_blocking_login(user) -> bool` = `role == member AND membership_status == cancelled`. **Esta función no mira los pagos**, y vive en otro módulo que la de 4.1 a propósito (dec. 4 del design): el color rojo y el bloqueo de acceso son reglas distintas y no deben poder acoplarse por descuido.
- [x] 4.2 En `app/schemas.py`: reemplazar `ClientBase`/`ClientCreate`/`ClientUpdate`/`ClientOut` por `UserBase`/`UserCreate`/`UserUpdate`/`UserOut` con el perfil completo; `UserOut` incluye `full_name`, `age`, `membership_cancelled_at`, `membership_indicator` (los 4 valores) e `invitation_status`. Renombrar `ClientStatus` → `UserPaymentStatus` y `EmbeddedClient` del lado de `PaymentOut`/`AttendanceOut` (`user` en vez de `client`, `user_id` en vez de `client_id`). Borrar `ClientRegisterIn`, `ClientPortalAccessCreate`, `ClientPortalAccessOut`.
- [x] 4.3 Actualizar `schemas.Role` a `Literal["owner", "coach", "member"]` y `WorkoutLogOut.client_id` → `user_id`, `ClientProgressSummary` → `UserProgressSummary` (`user_id`/`user_name`).
- [x] 4.4 En `app/deps.py`, agregar la dependencia `require_can_manage_user(target)` con la regla de permisos: owner gestiona cualquier rol; coach solo usuarios con `role == member` y solo puede asignar `member`. No reimplementar el chequeo endpoint por endpoint.

## 5. Endpoints de usuarios (reemplazo de `/clients` y `/coaches`)

- [x] 5.1 Renombrar `app/routers/clients.py` → `app/routers/users.py`, prefijo `/users`, y actualizar el registro en `app/main.py`.
- [x] 5.2 `GET /users`: filtros `q` (sobre `full_name`/`email`/`phone`), `role`, `membership_status`; paginación con `X-Total-Count` y `Link` como hoy; `order_by` sobre las columnas nuevas. El `membership_indicator` de toda la página sale de **una** subconsulta correlacionada en la misma query (nada de una consulta de pagos por fila).
- [x] 5.3 `POST /users` y `PATCH /users/{id}` usando `require_can_manage_user`; el owner puede cambiar el rol de un usuario existente, el coach no. Password opcional y solo seteable por el owner para roles no-miembro (preserva el comportamiento actual de `POST /coaches`; ver hueco de spec 3).
- [x] 5.4 `POST /users/{id}/membership/cancel` con body opcional `{ cancelled_at?: datetime }` (default `now()`, admite fecha retroactiva) y `POST /users/{id}/membership/activate`, que además **limpia `membership_cancelled_at`**. **No** exponer `membership_status`, `membership_cancelled_at` ni `is_active` como campos sueltos del `PATCH`.
- [x] 5.5 **Eliminar** `DELETE /clients/{id}` y no crear `DELETE /users/{id}`. Adaptar `GET /users/{id}/status` desde el `client_status` actual, reusando el helper de `utils.py`.
- [x] 5.6 Eliminar `GET/POST /clients/{id}/portal-access` (los reemplaza la invitación) y borrar `app/routers/coaches.py` completo, incluido su `DELETE /coaches/{id}` (contradice "no hay eliminación física").
- [x] 5.7 Rechazar escrituras de pago, asistencia y log de rutina sobre usuarios con `membership_status != 'active'` (requirement: "aplican únicamente a usuarios con membresía activa").

## 6. Repropagar `user_id` en el resto del backend

- [x] 6.1 `app/routers/payments.py`: `client_id` → `user_id` en el schema, el filtro de unicidad de período, el `join`/`contains_eager` (ahora contra `User`), el `q` de búsqueda y `func.count(distinct(...))` de los KPIs.
- [x] 6.2 `app/routers/attendance.py`: `client_id` → `user_id` en el filtro, el `joinedload`, la búsqueda por `q` y el `checkin` (que hoy resuelve un `models.Client` por `q`).
- [x] 6.3 `app/routers/routines.py`: `_get_client_or_404` → `_get_user_or_404`, `_get_user_client_or_403` pasa a resolver el propio `current_user` (ya no hay `client_id`: el miembro **es** el usuario), rutas `/routines/clients/{id}/...` → `/routines/users/{id}/...`, y `WorkoutLog.client_id` → `user_id` en queries y serialización. `require_role(UserRole.user)` → `UserRole.member`.
- [x] 6.4 `app/routers/reports.py`: el reporte `new_clients` pasa a contar `users.membership_start_date` de usuarios con perfil de miembro, en vez de `clients.join_date`.
- [x] 6.5 `app/routers/auth.py`: eliminar `POST /auth/client-register`; sacar el claim `client_id` de `_build_token_for_user`; agregar en `POST /auth/token` los **dos** gates con mensajes distintos — (a) `password_hash IS NULL` → "invitación pendiente de completar", **antes** de `verify_password` (llamarlo con hash `None` revienta); (b) `is_membership_blocking_login(user)` → "membresía dada de baja".
- [x] 6.6 Aplicar el gate (b) **también en `get_current_user`** (`app/auth.py`), devolviendo **401** (no 403) para que el interceptor de `frontend/src/lib/http.ts` desloguee limpio. Sin esto, dar de baja a un Miembro no lo saca del sistema hasta 10 h después (`ACCESS_TOKEN_EXPIRE_MINUTES = 600`).

## 7. Flujo de invitación (backend)

- [x] 7.1 Agregar el modelo `MemberInvitation` a `models.py` (dec. 9 del design): `user_id`, `email_token_hash`, `phone_token_hash`, `created_at`, `expires_at`, `email_verified_at`, `phone_verified_at`, `completed_at`, `revoked_at`, `created_by_user_id`, con índice único parcial sobre `user_id WHERE revoked_at IS NULL AND completed_at IS NULL`.
- [x] 7.2 Migración Alembic nueva para `member_invitations` (puede ser una segunda revisión encadenada a la de la fusión, para que la migración riesgosa quede aislada y revisable por separado).
- [x] 7.3 Crear `app/notifications.py` con la interfaz `NotificationSender`, la implementación `"log"` (default, la que usa la suite) y la `"smtp"` sobre `smtplib` de la biblioteca estándar; agregar `NOTIFICATIONS_BACKEND` y `SMTP_*` a `config.py`, `.env.example` y `.env.docker.example`. **Sin dependencias nuevas.**
- [x] 7.4 `POST /users/{id}/invitation` (owner/coach): genera dos tokens en claro, guarda solo sus SHA-256, `expires_at = now + 7 días`, revoca la invitación viva si existe. Rechaza con 400 si `role != member`, si falta `phone` o si falta `email`; con 409 si el usuario ya tiene `password_hash`. Devuelve los dos links en claro para que la UI los muestre/copie. Dispara el envío de email por el `NotificationSender`.
- [x] 7.5 `app/routers/invitations.py` (público, sin auth): `GET /invitations/{channel}/{token}` valida, marca **ese** canal como verificado y devuelve el estado (`404` token inexistente, `410` expirado o revocado, `409` ya completada).
- [x] 7.6 `POST /invitations/{channel}/{token}/complete` con `{ password }`: exige ambos canales verificados (si no, `409` indicando cuál falta), setea `password_hash` + `email_verified` + `phone_verified` en el usuario y `completed_at` en la invitación, y devuelve el JWT vía `_build_token_for_user` para dejar al miembro logueado. **Antes de emitir el token, pasar por `is_membership_blocking_login`**: si le dieron de baja la membresía mientras la invitación estaba en curso, guarda la contraseña pero rechaza el acceso con el mismo mensaje de baja (spec `member-invitation`: completar la invitación habilita el login solo mientras la membresía siga activa).
- [x] 7.7 Derivar `invitation_status` (`none | pending | expired | access_active`) en `UserOut`: `access_active` si `password_hash IS NOT NULL`; si no, según la invitación viva.

## 8. Tests de backend

- [x] 8.1 Actualizar `backend/tests/helpers.py` y `conftest.py` al modelo unificado (crear usuarios en vez de clientes; rol `member` en vez de `user`).
- [x] 8.2 Tests de permisos de ABM en `test_roles.py`: coach crea un miembro (OK), coach intenta crear un Dueño (403), coach intenta editar a otro Coach (403), owner cambia el rol de un usuario (OK), y que **no exista** ninguna ruta de borrado de usuario.
- [x] 8.3 Test del indicador de 3 estados (`payment-status-indicator`): membresía activa sin pagos → `overdue`; último pago del período actual o futuro → `up_to_date`; período anterior → `overdue`; membresía dada de baja → `suspended` **aunque tenga el pago del mes al día** (la baja prevalece); usuario que nunca fue miembro → `none`; registrar un pago del período actual y ver que el listado pasa de `overdue` a `up_to_date` sin tocar ningún caché; reactivar una membresía y ver que deja de ser `suspended` y vuelve a reflejar el pago.
- [x] 8.4 Tests del flujo de invitación: invitar sin celular (400), invitar a un Coach (400), verificar solo email deja el celular sin verificar, intentar definir contraseña con un canal pendiente (409 con el canal faltante), completar con ambos verificados (200 + token), link de más de 7 días (410), reenvío invalida el link anterior (410 sobre el viejo, el nuevo funciona), login con invitación pendiente (rechazado con el detalle correcto), y **baja posterior a una invitación ya completada vuelve a bloquear el login**.
- [x] 8.5 Tests del bloqueo de login por baja, **scopeado por rol** (invierte lo que decía la spec vieja): dar de baja a un usuario con **rol Miembro** que tiene password → `POST /auth/token` lo rechaza con el detalle de membresía dada de baja; reactivarlo → vuelve a entrar; dar de baja la condición de miembro de un usuario con **rol Coach** → **sigue entrando** y conserva sus permisos administrativos. En los tres casos el historial de pagos/asistencia/rutinas sigue existiendo.
- [x] 8.6 Test de que las dos reglas están desacopladas: un Coach-miembro dado de baja devuelve `membership_indicator == "suspended"` (rojo en el listado) **y a la vez** puede loguearse — es el escenario que la spec pone explícito y el que se rompe si alguien fusiona los dos checks.
- [x] 8.7 Test del enforcement en sesión abierta: con un token ya emitido para un usuario rol Miembro, darle de baja la membresía y verificar que la siguiente request autenticada devuelve **401** (no espera a que expire el token).
- [x] 8.8 Tests de la fecha de baja: baja sin body → `membership_cancelled_at` queda con la fecha/hora actual; baja con `cancelled_at` retroactivo → guarda esa fecha; reactivación → vuelve a `NULL`.

## 9. Frontend: tipos y capa de datos

- [x] 9.1 `src/types.ts`: `Client` → `User` con el perfil completo (`first_name`, `last_name`, `full_name`, `birth_date`, `age`, `weight_kg`, `height_cm`, `email`, `email_verified`, `phone`, `phone_verified`, `role`, `membership_status`, `membership_start_date`, `membership_cancelled_at`, `created_at`, `is_active`, `membership_indicator`, `invitation_status`); `EmbeddedClient` → `EmbeddedUser`; `Payment`/`Attendance`/`WorkoutLog` pasan a `user_id`/`user`; `Role` pasa a `"owner" | "coach" | "member"`.
- [x] 9.2 Renombrar `src/services/clients.ts` → `users.ts` y `clients.queries.ts` → `users.queries.ts` (`useUsersQuery`, `useCreateUserMutation`, `useUpdateUserMutation`, `useCancelMembershipMutation`, `useActivateMembershipMutation`, `useInviteUserMutation`), apuntando a `/users`. Mantener el patrón fetchers puros + hooks y el default export de `@/lib/http`.
- [x] 9.3 `src/services/queryKeys.ts`: `clients` → `users`. Verificar que las mutaciones sigan invalidando por prefijo cruzado (`users` + `payments` + `attendance`, porque esas respuestas embeben la persona).
- [x] 9.4 `src/services/search.ts` y `payments.ts`: apuntar a `/users`, renombrar `searchClients` → `searchUsers`, y actualizar `getPaidClientIds`/`getPendingClients` a `user_id` (se mantienen: responden "quién no pagó *ese* período", distinto del indicador del listado).
- [x] 9.5 `src/stores/session.ts`: agregar `normalizeRole()` que mapee el legacy `"user"` → `"member"` (mismo patrón que `normalizeThemeMode`), para que las sesiones abiertas antes del deploy no queden sin rol ni rutas.

## 10. Frontend: vista de Usuarios y ABM

- [x] 10.1 Renombrar `src/pages/Clients.tsx` → `src/pages/Users.tsx`; actualizar la clave de `src/lib/routePreload.ts` (`/clients` → `/users`) — es la misma factory que usan el `lazy()` de `App.jsx` y el preload en hover del Sidebar, si se desincronizan el preload se rompe en silencio.
- [x] 10.2 En `App.jsx`: `<Route path="/users">` con `ProtectedRoute roles={["owner","coach"]}`, más `<Route path="/clients" element={<Navigate to="/users" replace />} />` para bookmarks; actualizar los dos `role === "user"` a `"member"`.
- [x] 10.3 Tabla del listado con las columnas de la spec (nombre completo, rol, fecha de alta, fecha de comienzo en el gimnasio — vacía o "-" si `membership_status === 'none'`), más la columna Acciones. El círculo va **antes del nombre**, mapeando directo `membership_indicator` del servidor: `up_to_date` → verde, `overdue` → naranja, `suspended` → rojo, `none` → sin círculo. **Sin lógica condicional propia** (nada de combinar estado de membresía con estado de pago en el componente: esa precedencia ya la resolvió el backend). El círculo lleva `title`/`aria-label` con el estado en texto — verde/naranja/rojo sin alternativa textual no es accesible.
- [x] 10.4 Renombrar `src/components/EditClientDialog.tsx` → `EditUserDialog.tsx`: campos de perfil nuevos (nombre, apellido, fecha de nacimiento con la edad derivada mostrada, peso, altura, email, celular), selector de rol visible solo para el owner, sección de membresía (estado, fecha de comienzo, **fecha de baja**, "Dar de baja la membresía" con **selector de fecha opcional** que por default usa el momento actual, y "Reactivar"). **Borrar** el bloque "Acceso del cliente (vista USER)" y no incluir ninguna acción de eliminar. Pasar de `api.patch` directo a `useUpdateUserMutation` (convención del repo que el archivo hoy incumple).
- [x] 10.4b En la confirmación de baja, el copy tiene que decir la consecuencia real según el rol: para un **rol Miembro**, que además pierde el acceso a la aplicación; para un Dueño/Coach marcado como miembro, que **conserva** su acceso administrativo y solo sale del seguimiento de pagos/asistencia/rutinas. Es la única superficie donde esa asimetría le queda visible a quien ejecuta la acción.
- [x] 10.5 Agregar al diálogo la sección de invitación: estado (`Sin invitar` / `Invitación pendiente` / `Acceso activo`), botón Invitar/Reenviar, copiar link y botón que abre `https://wa.me/<phone>?text=<link>` — mismo patrón que el recordatorio de pago ya existente.
- [x] 10.6 Rename de navegación a "Usuarios": `Sidebar.tsx` (item), `Topbar.tsx` (menú mobile), `SpotlightSearch.tsx` (heading del `CommandGroup` y el copy del placeholder).
- [x] 10.7 Actualizar los consumidores restantes de `Client`: `pages/Dashboard.tsx`, `pages/UserRoutine.tsx`, `components/UserCard.tsx` (incluye las rutas `/routines/clients/...` → `/routines/users/...`), `components/SpotlightSearch.tsx` y `hooks/useDashboardData.ts`. También actualizados por el mismo cambio de contrato (no listados explícitamente pero rotos si no se tocan): `pages/Attendance.tsx`, `pages/NewCoach.tsx` (`/coaches` → `POST /users` con `role=coach`), `components/AttendanceCalendar.tsx`, `components/LastPayments.tsx`, `components/NewPaymentDialog.tsx`, `services/me.ts`, `services/attendance.ts`, `lib/http.ts`, `test/apiMock.ts`, `components/ProtectedRoute.tsx`.

## 11. Frontend: pantalla pública de invitación y retiro de `/register-client`

- [x] 11.1 Crear `src/pages/InvitationAccept.tsx` en `/invitacion/:channel/:token`: al montar llama al `GET`, muestra el estado de los dos canales, y habilita el formulario de contraseña solo cuando ambos están verificados; maneja los casos expirado / revocado / ya completado con mensajes propios. Estilo heredado de la vista de login (es el reemplazo funcional de `register-client-view`).
- [x] 11.2 En `App.jsx`, agregar `/invitacion` a la condición de `isAuthRoute` (hoy es un `startsWith` sobre `/login` y `/register-client`) — sin eso la pantalla se renderiza dentro del shell autenticado y `ProtectedRoute` la manda al login.
- [x] 11.3 Borrar `src/pages/RegisterClient.tsx`, `src/pages/__tests__/RegisterClient.test.tsx`, el `lazy()` y la `<Route path="/register-client">` de `App.jsx`, y su rama en `isAuthRoute`.
- [x] 11.4 Quitar el botón "Registrar cuenta" de `src/pages/Login.tsx` (línea 166), que quedaría navegando a una ruta inexistente. Cubierto por la delta spec `specs/login-view/spec.md` de este change (REMOVED "Link a registro de cuenta").

## 12. Tests de frontend

- [x] 12.1 Agregar un test de render de `Users.tsx` con el helper `apiMock`: las cuatro columnas de la spec y los cuatro estados del círculo — verde (`up_to_date`), naranja (`overdue`), rojo (`suspended`) y sin círculo (`none`) — afirmando sobre el texto accesible del indicador, no sobre la clase de color.
- [x] 12.2 Agregar un test de render de `InvitationAccept.tsx`: con un solo canal verificado el formulario de contraseña está deshabilitado y se indica qué canal falta; con ambos verificados se habilita.
- [x] 12.3 Actualizar `components/__tests__/Sidebar.test.tsx` (assert "Usuarios" presente y "Clientes" ausente para owner/coach) y `Topbar.test.tsx`/`Dashboard.test.tsx` (revisados: ninguno de los dos referencia el label viejo "Clientes" ni el rol legacy `"user"`, así que no necesitaban cambios para seguir pasando).
- [x] 12.4 Correr `make test` (backend + frontend) y `npm run lint` dentro de `frontend/`, y dejar la suite en verde.

## 13. Scripts, docs y cierre

- [x] 13.1 Adaptar los scripts de `backend/scripts/` que siguen teniendo sentido (`seed_clients.py` → `seed_users.py`, `import_clients_csv.py` → `import_users_csv.py` con su template CSV, `reset_clients.py`, `seed_payments.py`, `seed_attendance.py`) y **borrar `mark_coaches_verified.py`** (su razón de existir la absorbe el paso 3 de la migración). También adaptados `dump_payments.py` y `create_owner.py` (no estaban en la lista pero quedaban rotos por `client_id`/`full_name` ya no asignable).
- [x] 13.2 Actualizar `backend/AGENTS.md` (los tres roles del enum pasan a `owner`/`coach`/`member`; nota de los tests nuevos) y `frontend/AGENTS.md` (servicios `users.*`, vista `Users.tsx`, tests nuevos), según la regla del repo de documentar tests en el `AGENTS.md` de cada app.
- [ ] 13.3 Ejecutar el plan de migración del design contra producción: pre-vuelo (incluida la lista de 1.1b, revisada con el gimnasio) → snapshot → `make migrate` → deploy conjunto de backend y frontend → verificación post-deploy (login owner, login coach, listado con los tres colores, un pago histórico sigue asociado a su persona, historial de rutinas de un miembro migrado, y el caso asimétrico: Miembro dado de baja no entra / Coach-miembro dado de baja sí entra).
- [x] 13.4 Dejar anotado como seguimiento el borrado de la columna temporal `legacy_client_id` y la limpieza de copy "Clientes" en las specs que el proposal dejó fuera de alcance. Anotado en el comentario de `legacy_client_id` en `models.py` (apunta a design.md decisión 6 y al Migration Plan paso 7); la limpieza de copy ya está registrada en el proposal ("Deuda de copy identificada pero fuera de alcance") y en design.md.
