## 1. Tokens y utilidad de contenedor (`index.css`)

Base de todo lo demás: sin estos tokens, ni el shell ni la plantilla tienen de dónde leer las
alturas. Nada de este bloque cambia lo que se ve. Ver design dec. 1, 2 y 6.

- [x] 1.1 En `frontend/src/index.css`, bloque `@theme inline` (junto a `--spacing-sidebar`, línea ~107): agregar `--spacing-topbar: 4rem` y `--spacing-page-y: 1.5rem`, con comentario que apunte a `docs/design/design.md` como el resto de los tokens del archivo (dec. 2).
- [x] 1.2 En el mismo bloque, agregar la var derivada `--list-page-height: calc(100dvh - var(--spacing-topbar) - 2 * var(--spacing-page-y))`, con el comentario de por qué se define acá y no como `calc` inline en un className (dec. 6.1).
- [x] 1.3 En `@layer utilities` (línea ~227), agregar `.app-container` con `@apply mx-auto w-full max-w-[var(--container-app)] px-4 sm:px-6 lg:px-8`, más el comentario de que el gutter vertical queda deliberadamente afuera (dec. 1).
- [x] 1.4 Verificar que las utilidades generadas existen: `npm run build` dentro de `frontend/` no debe fallar y `h-topbar`/`py-page-y` deben resolver (si un nombre de token está mal, Tailwind no genera la clase y el build pasa igual pero la clase no aplica — chequear en el CSS emitido).

## 2. Shell: contenedor y alturas (`App.jsx`, `Topbar.tsx`, `Sidebar.tsx`)

Cierra el requirement "Alineación del Topbar con el contenedor de contenido" (`specs/app-shell`).
Ver design dec. 1, 2 y 4.

- [x] 2.1 `frontend/src/App.jsx`: el `div` contenedor del `<Suspense>` pasa a `className="app-container py-page-y"`, reemplazando `mx-auto w-full max-w-[var(--container-app)] px-4 py-6 sm:px-6 lg:px-8` (dec. 1).
- [x] 2.2 `frontend/src/components/Topbar.tsx:101`: el `div` interior pasa a `className="app-container flex h-topbar items-center justify-between"` — gana el `max-w` que hoy le falta (esa es la causa raíz de la desalineación) y cambia `h-14` por `h-topbar` (dec. 1 y 2).
- [x] 2.3 `frontend/src/App.jsx:76`: en `<main>`, sacar `pt-14` y cambiar `min-h-screen` por `min-h-[calc(100dvh-var(--spacing-topbar))]`; conservar `lg:pl-sidebar`. Reescribir el comentario de las líneas 70-75, que hoy se contradice ("sticky sí ocupa lugar en el flujo" + "pt-14 compensa su altura") (dec. 4).
- [x] 2.4 Verificar en navegador (`make dev`) que el contenido no quedó tapado por el Topbar tras sacar el `pt-14`. Si quedó tapado, el Topbar no está en flujo: reponer `pt-topbar` y anotarlo en el reporte de la task (dec. 4, riesgo declarado).
- [x] 2.5 `frontend/src/components/Sidebar.tsx:63-80`: sacar la placa de marca del `div.space-y-6.p-4` y ponerla en una banda de encabezado propia con `h-topbar` + `border-b border-border`; el logo baja de `h-10 w-10` a `h-9 w-9`; la placa conserva `warm-accent-bg warm-glow rounded-xl`. El nav queda en su propio bloque con `p-4` (dec. 2).
- [x] 2.6 Correr `make test-frontend` y confirmar que `Sidebar.test.tsx` y `Topbar.test.tsx` siguen en verde (afirman sobre texto renderizado, no sobre layout: no deberían romperse).

## 3. Fuente única de navegación (`lib/navigation.ts`)

Cierra el scenario "Nombre de la sección actual visible en desktop". Ver design dec. 3.

- [x] 3.1 Crear `frontend/src/lib/navigation.ts` con el tipo `NavItem` y la constante `NAV_ITEMS`, mudando literal el array `items` de `Sidebar.tsx:23-32` (mismos `to`, `label`, `icon`).
- [x] 3.2 En el mismo archivo, agregar `sectionTitleForPath(pathname: string): string | null`, que resuelve por **prefijo más largo que matchea** (`/users/u-1` → "Usuarios") y devuelve `null` sin match (dec. 3).
- [x] 3.3 `Sidebar.tsx`: importar `NAV_ITEMS` desde `lib/navigation` y borrar el array local; el filtro por rol (`allowed`) se queda donde está.
- [x] 3.4 `Topbar.tsx`: renderizar `sectionTitleForPath(location.pathname)` en el slot izquierdo de desktop (hoy `lg:hidden`, vacío en desktop), como `<p>` y **no** como heading — el `h1` es de la vista (dec. 3). Si devuelve `null`, no renderizar nada.
- [x] 3.5 `Topbar.tsx:190-247`: los 7 botones del menú mobile pasan a derivarse de `NAV_ITEMS` en vez de repetir labels e iconos literales, respetando el filtrado por rol que ya hace hoy (member → solo "Mi rutina"; reportes solo owner).
- [x] 3.6 Crear `frontend/src/lib/__tests__/navigation.test.ts`: `sectionTitleForPath` para `/users` ("Usuarios"), `/users/u-1` (prefijo → "Usuarios") y una ruta desconocida (`null`); más el test de drift: toda entrada de `NAV_ITEMS` tiene su `to` presente en `routeImporters` de `lib/routePreload.ts` (dec. 3).

## 4. `Popover` sin radix (`components/ui/popover.tsx`)

Cierra los scenarios de apertura/cierre/foco del requirement "Leyenda de roles y estados en
popover". Bloque aislado: no depende de los grupos 1-3. Ver design dec. 5.

- [x] 4.1 Crear `frontend/src/components/ui/popover.tsx` con `Popover` (context + `open`/`onOpenChange`, wrapper `relative inline-flex`), `PopoverTrigger` (`<button>` real, con `aria-haspopup="dialog"`, `aria-expanded`, `aria-controls`) y `PopoverContent` (`role="dialog"`, `id`, `aria-labelledby`, `tabIndex={-1}`), siguiendo el patrón controlado de `dialog.tsx` (dec. 5).
- [x] 4.2 `PopoverContent` retorna `null` cuando está cerrado (nada oculto pero focuseable en el DOM) y se posiciona `absolute` con `align="start" | "end" | "center"`; sin `side`, siempre debajo (dec. 5).
- [x] 4.3 Implementar el cierre: `keydown` de Escape en `document`, `pointerdown` afuera del wrapper (no `click`), y `focusout` con `relatedTarget` fuera. En los tres casos, `onOpenChange(false)` (dec. 5).
- [x] 4.4 Implementar el foco: al abrir, foco al contenido; al cerrar por Escape o click afuera, **retorno explícito al trigger** vía `triggerRef.current?.focus()` (scenario "Cerrar el popover con Escape devuelve el foco").
- [x] 4.5 Estilos: `rounded-xl border border-border bg-surface-1 p-4 shadow-[var(--shadow-level-2)]` (Level 2 del design doc, fondo **opaco**) + `animate-in fade-in-0 zoom-in-95 duration-150` al abrir, sin animación de salida.
- [x] 4.6 Documentar en un comentario de cabecera, al estilo de `dialog.tsx`, la limitación de no tener portal: un `PopoverContent` dentro de un ancestro con `overflow: auto/hidden` queda clippeado, así que no se usa dentro del cuerpo scrollable de una tabla (dec. 5).
- [x] 4.7 Crear `frontend/src/components/ui/__tests__/popover.test.tsx`: cerrado no renderiza contenido; click en el trigger abre y pone `aria-expanded="true"`; Enter y Espacio sobre el trigger abren; Escape cierra y el foco vuelve al trigger; click afuera cierra (dec. 11).

## 5. Primitivos de tabla y paginación

Preparan lo que consume la plantilla. Ver design dec. 7 y 8.

- [x] 5.1 `frontend/src/components/ui/table.tsx`: `Table` acepta `containerClassName?: string` y lo compone con `cn()` sobre el `div` wrapper (`relative w-full overflow-x-auto`). Cambio aditivo, 0 callers hoy (dec. 7).
- [x] 5.2 `frontend/src/services/pagination.ts`: agregar `getPageRange({ total, limit, offset }) => { page, pages, from, to }`, extrayendo la aritmética hoy inline en `Pagination.tsx:9-20`. No tocar `PaginatedResult` ni `readTotalCount` (dec. 8).
- [x] 5.3 `frontend/src/components/Pagination.tsx`: consumir `getPageRange` y agregar la prop opcional `showRange?: boolean` con **default `true`**, que condiciona solo el bloque de texto "Mostrando X-Y de Z" (dec. 8).
- [x] 5.4 Verificar que `frontend/src/pages/Attendance.tsx:272` sigue renderizando igual **sin tocar el archivo**: el default `true` es lo que lo mantiene fuera de alcance (dec. 8).
- [x] 5.5 Crear `frontend/src/services/__tests__/pagination.test.ts` para `getPageRange`: `total=0`, última página parcial y `offset` fuera de rango (dec. 11).

## 6. Plantilla `ListPageLayout`

Cierra el requirement "Tabla de Usuarios con scroll interno, encabezado fijo y paginación única" y
la parte estructural de "Encabezado compacto". Ver design dec. 6.

- [x] 6.1 Crear `frontend/src/components/ListPageLayout.tsx` con los 5 slots: `title`, `titleAdornment`, `count`, `description`, `primaryAction`, `toolbar`, `footer` y `children`. Componente de layout puro: sin estado ni datos (dec. 6).
- [x] 6.2 Header en **una sola fila** (título + adorno + pill de total + descripción a la izquierda; acción primaria a la derecha); `title` se renderiza como el `<h1>` de la vista; `description` se oculta debajo de `md` (dec. 6).
- [x] 6.3 Columna: `section.flex.flex-col.gap-4` + `lg:h-[var(--list-page-height)]`. El alto fijo aplica **solo desde `lg:`**; debajo la sección es de alto automático y el scroll vuelve a ser el de la página (dec. 6.3).
- [x] 6.4 Marco de la tabla: `flex-1 min-h-0 overflow-hidden rounded-xl border border-border`. El `min-h-0` no es opcional — sin él el ítem flex no se encoge y `overflow-auto` no scrollea (dec. 6.2).
- [x] 6.5 Footer con `shrink-0` para que la paginación nunca se salga de pantalla. El componente **no** define `mx-auto`, `max-w` ni `px-*`: los hereda del contenedor del shell (dec. 6.4).
- [x] 6.6 Documentar en comentario la regla de la acción primaria icon-only: el caller pasa el botón con `aria-label` **fijo** + `<span className="hidden md:inline">`, usando `md:` (768px) y no `sm:` (640px). Sin el `aria-label` fijo el botón se queda sin nombre accesible en mobile, porque `hidden` es `display:none` (dec. 6).
- [x] 6.7 Crear `frontend/src/components/__tests__/ListPageLayout.test.tsx`: los slots se renderizan en el orden esperado, el `title` sale como `h1`, el `count` y la acción primaria están en el header, y `children` queda dentro del marco (dec. 11).

## 7. Migración de `Users.tsx`

Cierra el resto de los requirements de `specs/user-management`. Ver design dec. 9.

- [x] 7.1 Extraer `MembershipDot` de `Users.tsx:55-65` a `frontend/src/components/MembershipDot.tsx` (junto con `INDICATOR_LABEL` e `INDICATOR_DOT_CLASS`), sin cambiarle el comportamiento: sigue devolviendo `null` para `none` y exponiendo el texto accesible (`role="img"` + `aria-label`).
- [x] 7.2 Borrar de `Users.tsx` el hero `hero-aura` y la card de Leyenda (líneas 166-215), incluido el grid `lg:grid-cols-[1.35fr_0.85fr]` que las contenía (scenario "Sin hero de bienvenida", "La leyenda no es una card visible por defecto"). No tocar la utilidad `.hero-aura` de `index.css`: la siguen usando Dashboard, Reports y NewCoach.
- [x] 7.3 Crear el componente local `LegendPopover` dentro de `Users.tsx`, con `LEGEND_ROLES`/`LEGEND_INDICATORS` y el trigger de ícono `Info` con `aria-label` que identifique su propósito. `PopoverContent` con ancho fluido `w-[min(18rem,calc(100vw-2rem))]` — mismo popover en todos los viewports, sin drawer (po-answers #6).
- [x] 7.4 Envolver la vista en `<ListPageLayout>` con `title="Directorio de usuarios"` (distinto del "Usuarios" del Topbar), `count` con el total, descripción corta, `titleAdornment={<LegendPopover/>}` y `primaryAction` = botón "Crear usuario" con la regla icon-only de 6.6 (dec. 9).
- [x] 7.5 Toolbar: el input de búsqueda **sin** el label en mayúsculas "BUSCAR" (líneas 229-231), con `aria-label="Buscar usuarios"` + `placeholder` + ícono `Search` (requirement "Filtro de búsqueda sin etiqueta en mayúsculas").
- [x] 7.6 Reemplazar el `<table>` a mano (líneas 261-358) por los primitivos de `components/ui/table.tsx`, con `containerClassName="h-full overflow-auto"` (dec. 7).
- [x] 7.7 Encabezado de columnas fijo: `sticky top-0 z-10` en cada **`th`** (no en el `thead`: con `border-collapse: collapse` varios motores lo ignoran), fondo **opaco** `bg-surface-1` (hoy es `bg-canvas/70 backdrop-blur-xl` y dejaría ver las filas al scrollear) y línea inferior con `shadow-[inset_0_-1px_0_var(--border-hairline)]` (dec. 7).
- [x] 7.8 Columnas nuevas con los headers literales de la spec: Nombre / Contacto / Rol / Alta / Inicio en el gimnasio / Acciones. Contacto = `email` → si no hay, `phone` → si no hay, `"-"`, con `truncate` + `title` con el valor completo.
- [x] 7.9 Borrar el UUID de la segunda línea de la celda Nombre (línea 308) — scenario "Sin UUID visible en la fila".
- [x] 7.10 Acciones por fila como icon-buttons (`Button` `size="icon"` `variant="outline"`), cada uno con `aria-label` **y** `title` que incluyan el nombre del usuario ("Ver perfil de {nombre}", "Editar {nombre}", "Enviar recordatorio por WhatsApp a {nombre}"). WhatsApp `disabled` sin `phone`, con `title` explicando por qué (dec. 9).
- [x] 7.11 Actualizar el `colSpan` de los tres estados (skeleton, `DataError`, `EmptyState`) de 5 a **6**, y agregar la celda de Contacto al `SkeletonRow`. Los tres estados se conservan tal cual (proposal: "se conservan los estados de skeleton, error y vacío").
- [x] 7.12 Dejar **una sola** `Pagination`, en el `footer` del layout y con `showRange={false}` (el total ya está en el header); borrar la del bloque de filtros (líneas 244-252) — scenario "Una sola paginación visible".
- [x] 7.13 `frontend/src/index.css`: agregar los tres tokens de estado por modo, con comentario que apunte al requirement "Contraste accesible de indicadores de estado y roles" y a los ratios medidos (dec. 12). En `:root` (dark, **sin cambio visual**): `--status-ok: oklch(0.696 0.17 162.48)` y `--status-warn: oklch(0.769 0.188 70.08)` — los mismos oklch que la paleta v4 usa para emerald-500/amber-500, que es lo que hay hoy — más `--status-danger: var(--destructive)`. En `:root[data-theme="light"]`: `--status-ok: oklch(0.508 0.118 165.612)` (emerald-700, ≈5.3:1 sobre `#ffffff`), `--status-warn: oklch(0.555 0.163 48.998)` (amber-700, ≈5.0:1) y `--status-danger: var(--destructive)` (≈4.8:1, ya pasaba). Mapearlos en `@theme inline` como `--color-status-ok|warn|danger` para que existan `bg-status-ok|warn|danger`.
- [x] 7.14 `frontend/src/components/MembershipDot.tsx`: `INDICATOR_DOT_CLASS` pasa a ser enteramente semántico — `up_to_date: "bg-status-ok"`, `overdue: "bg-status-warn"`, `suspended: "bg-status-danger"`, reemplazando las clases crudas `bg-emerald-500`/`bg-amber-500` que `frontend/AGENTS.md` prohíbe. No cambia el markup, el `role="img"`, el `aria-label` ni el tamaño del punto: `Users.test.tsx` afirma sobre el texto accesible, no sobre el color, así que no debería romperse (dec. 12).

## 8. Tests de la vista

Ver design dec. 11.

- [x] 8.1 `frontend/src/pages/__tests__/Users.test.tsx`: actualizar el caso de columnas a los headers nuevos ("Nombre", "Contacto", "Rol", "Alta", "Inicio en el gimnasio", "Acciones"); ya no aplica el comentario de las líneas 63-66 sobre escopear a la tabla por la leyenda del hero (la leyenda dejó de estar montada por default).
- [x] 8.2 Mismo archivo: actualizar el caso "el boton Ver de una fila navega a la ficha" — el selector `getByRole("button", { name: /^ver$/i })` deja de matchear al pasar a icon-button con `aria-label` ("Ver perfil de …").
- [x] 8.3 Mismo archivo, casos nuevos de contacto: con email muestra el email; sin email y con teléfono muestra el teléfono; sin ninguno muestra `"-"`.
- [x] 8.4 Mismo archivo, caso nuevo: la fila no muestra el UUID del usuario.
- [x] 8.5 Mismo archivo, casos nuevos de leyenda: no está visible al abrir la vista, y aparece al hacer click en el ícono de información.
- [x] 8.6 Mismo archivo, caso nuevo: el título de la página es "Directorio de usuarios" (y no repite "Usuarios").
- [x] 8.7 Mismo archivo, casos nuevos de WhatsApp: deshabilitado cuando `phone` es `null`, habilitado cuando tiene teléfono.
- [x] 8.8 Mismo archivo, caso nuevo: hay un único control de paginación en la vista.
- [x] 8.9 `frontend/src/components/__tests__/Topbar.test.tsx`: caso nuevo — montado en la ruta `/users`, el Topbar muestra "Usuarios" como nombre de sección (dec. 3).

## 9. Documentación de diseño

Ver design dec. 10.

- [x] 9.1 `docs/design/design.md`, front-matter bloque `spacing`: agregar `topbar-height: 4rem` y `page-gutter-y: 1.5rem`, junto a `sidebar-width` y `gutter`.
- [x] 9.2 Sección Components → "2. Operational Hero Greeting Card": dejar explícito que es un patrón **exclusivo del Dashboard** y que ninguna vista de listado lo usa.
- [x] 9.3 Sección Components → "1. Navigation Elements": documentar que la Brand Plaque y la Top Utility Bar comparten alto y borde inferior (`topbar-height`), y que la barra usa el mismo contenedor de ancho máximo que el contenido.
- [x] 9.4 Sección Components → nueva subsección "6. List Page": las 4 zonas (header compacto de una fila / toolbar de filtros / cuerpo con scroll propio y encabezado fijo / pie de paginación único), el alto `calc(100dvh - topbar - 2×gutter)`, el corte en `lg` para volver al scroll de página en mobile, headers de columna en `label-caps`, acciones por fila como icon-buttons, acción primaria icon-only debajo de 768px, y la nota de que Pagos y Asistencias adoptan este patrón al reimplementarse.
- [x] 9.5 Sección Components → nueva entrada "Popover": superficie Level 2, fondo opaco, `rounded-xl`, no modal, sin portal.
- [x] 9.6 `docs/design/design.md`: documentar los colores de estado, que hoy **no existen en el documento** — esa ausencia es la causa raíz del hueco de contraste (dec. 12). Agregar `status-ok`/`status-warn`/`status-danger` al front-matter `colors` con el valor de cada modo, y una nota en "Colors" explicando que en light usan el paso 700 (no el 500) por el mínimo de 3:1 de WCAG 1.4.11 para objetos gráficos, mismo criterio que ya justifica la existencia de `--primary-strong`.

## 10. Verificación

Las dos primeras son **manuales**, con la app corriendo (`make dev`): jsdom no hace layout, así que
ninguno de estos scenarios es automatizable en Vitest (dec. 11).

- [x] 10.1 **[Manual, `make dev`]** Alineación en desktop, dark y light, a ≥1280px y a >1840px (donde el `max-w` del contenedor empieza a morder): el borde izquierdo del nombre de sección del Topbar coincide con el borde izquierdo del contenido de `main`, el borde derecho del toggle de tema con el derecho del contenido, y el borde inferior del Topbar forma una línea continua con el de la placa de marca del Sidebar (`specs/app-shell`, scenarios de bordes y de altura; ver Open Question del design sobre el buscador global).
- [x] 10.2 **[Manual, `make dev`]** Vista Usuarios en 1440x900 con 10 filas: encabezado, filtros, tabla completa y paginación visibles **sin scroll de página**; con más filas que el alto disponible, solo scrollea la tabla y el encabezado de columnas queda fijo; el popover de leyenda abre y cierra; debajo de 768px la acción primaria queda solo con ícono; contraste AA de badges de rol y puntos de estado en tabla y en popover, en dark y en light (scenarios de "Tabla … scroll interno", "Acción primaria solo con ícono en mobile", "Contraste accesible…"). **Bloqueada por 7.13 y 7.14**: la parte de contraste se re-mide **después** de esos dos cambios, con el mismo método de compositing real (oklch→sRGB sobre el fondo efectivo) que detectó el problema, y hay que confirmar los tres puntos ≥3:1 en light y ninguna regresión en dark.
  - **Fix de "tabla completa" (fila 9/10 cortada)**: icon-buttons de acciones `size="icon"` (36px) → `size="icon-sm"` (32px) y celdas de datos `p-4` → `px-4 py-2`, en `Users.tsx` (y `SkeletonRow` a la par, mismo alto). Medido en 1440x900 con las 10 filas reales de la DB: **alto de fila 69px → 49px**; contenedor de la tabla `scrollHeight=621 === clientHeight=621` (**sin scroll interno**, antes `scrollHeight` excedía y cortaba las filas 9-10); `getBoundingClientRect().bottom` de la fila 10 = 736, por debajo del `top` del footer de paginación (845) → fila 10 completa y visible antes del footer. Con 14 filas (todo el dataset de dev, tras subir el page size) el contenedor sí scrollea internamente (`scrollHeight=725 > clientHeight=621`) y el `th` sticky mantiene el mismo `top` (207px) antes/después de `scrollTop=104` → confirmado que el sticky sigue funcionando con más filas que las que entran. Capturas reemplazadas: `10.2-users-1440x900-dark.png`, `10.2-users-1440x900-light-v2.png`.
- [x] 10.3 `npm run lint` dentro de `frontend/` sin errores nuevos.
- [x] 10.4 `make test-frontend` en verde, con los 6 archivos de test nuevos/actualizados incluidos.

## 11. Revisión del dueño

Cinco cambios pedidos por el dueño sobre la implementación ya andando, más su documentación y
re-verificación. Ver design dec. 13-17. Orden por dependencia: primero el shell (11.1-11.4),
después la vista (11.5-11.9), después docs y verificación.

- [x] 11.1 `frontend/src/components/Topbar.tsx`: borrar el `const sectionTitle = sectionTitleForPath(...)` (línea ~102) y el `div.hidden.items-center.lg:flex` que lo renderiza (~122-125, incluido su comentario). El slot izquierdo queda vacío en desktop, como antes del change. Quitar el import de `sectionTitleForPath` (dec. 13).
- [x] 11.2 `frontend/src/lib/navigation.ts`: borrar `sectionTitleForPath` (líneas 35-52) — queda sin caller tras 11.1, y la convención del repo es borrar el huérfano, no dejar el export. **Conservar `NAV_ITEMS`** y el tipo `NavItem`, que siguen consumidos por `Sidebar.tsx` y por `navItemsForRole` (`Topbar.tsx:18`). Actualizar el comentario de cabecera (líneas 12-17), que hoy dice que `Topbar` lo usa "para el título de sección" (dec. 13).
- [x] 11.3 `frontend/src/lib/__tests__/navigation.test.ts`: borrar los 3 casos de `sectionTitleForPath` (ruta exacta, prefijo más largo, `null` sin match) y **conservar** el de drift "toda entrada de NAV_ITEMS tiene su `to` presente en routeImporters" (dec. 13).
- [x] 11.4 `frontend/src/components/__tests__/Topbar.test.tsx`: borrar el caso "montado en /users, muestra 'Usuarios' como nombre de seccion en el slot izquierdo" (línea 26). El caso del toggle de tema queda intacto (dec. 13).
- [x] 11.5 `frontend/src/components/Sidebar.tsx:42`: reemplazar `className="flex h-topbar items-center border-b border-border px-4"` por un contenedor con padding propio y sin alto fijo (p. ej. `px-4 pt-4 pb-2`) — se va la línea horizontal a la altura del Topbar y la placa recupera margen superior. Reescribir el comentario de las líneas 37-41, que justifica el `h-topbar` y el borde con la simetría de la dec. 2 y quedaría mintiendo (dec. 16 y 17). El logo queda en `h-9 w-9`: el dueño no pidió revertirlo a 40px.
- [x] 11.6 `frontend/src/components/ListPageLayout.tsx`: el marco de la tabla (línea 78) pasa de `div` a `Card` de `@/components/ui/card`, con `className="flex-1 min-h-0 gap-0 overflow-hidden py-0"`. Los `py-0 gap-0` no son opcionales: `Card` trae `py-6 gap-6` (`card.tsx:10`) y sin anularlos la tabla arranca 24px más abajo y se recorta el alto útil que la task 10.2 ya tuvo que pelear. Mantener el comentario del `min-h-0` (dec. 15). — **Revertida por 11.14**: el dueño corrigió el alcance de la Card, así que este marco vuelve a ser un `div` y la Card sube a la raíz.
- [x] 11.7 `frontend/src/components/ListPageLayout.tsx`: eliminar el slot `description` — la prop del tipo (línea 30), el destructuring (41) y el `<p className="hidden ... md:block">` (62-66). Actualizar el comentario de cabecera si lo menciona (dec. 14).
- [x] 11.8 `frontend/src/pages/Users.tsx`: `title="Usuarios"` en vez de `"Directorio de usuarios"` (línea 231) y borrar la prop `description` (234). En `STICKY_HEAD_CLASS` (125-126), `bg-surface-1` → `bg-card`, para atar el fondo del `th` sticky al mismo token que la Card; hoy resuelven al mismo valor, así que no hay cambio visual (dec. 14 y 15).
- [x] 11.9 Tests de la vista: en `ListPageLayout.test.tsx` sacar la aserción de `description` del caso "no renderiza los slots opcionales que no se pasan" (y del primer caso si la pasa); en `Users.test.tsx:74` el caso del título pasa a esperar el `h1` "Usuarios" y deja de afirmar que no repite el nombre de sección (dec. 13 y 14).
- [x] 11.10 `docs/design/design.md`, "1. Navigation Elements": corregir lo que escribió la task 9.3 — la Brand Plaque y la Top Utility Bar **ya no** comparten alto ni borde inferior; la placa lleva margen superior y sin borde, y `topbar-height` gobierna solo el Topbar (dec. 16 y 17). Mantener que la barra usa el mismo contenedor de ancho máximo que el contenido.
- [x] 11.11 `docs/design/design.md`, "6. List Page": sacar la descripción corta del header (el patrón queda título + total + acción primaria), documentar que el cuerpo de la tabla va dentro de una Card Level 1 con el padding vertical anulado, y quitar cualquier mención al nombre de sección en el Topbar si la subsección o "Layout & Spacing" la tienen (dec. 13, 14 y 15).
- [x] 11.12 **[Manual, `make dev`]** Re-verificación en 1440x900, dark y light: no se ve ninguna línea horizontal en el Sidebar a la altura del Topbar; la placa de marca tiene margen superior y no queda pegada al borde; el encabezado de Usuarios dice "Usuarios" sin subtítulo; la tabla y su encabezado de columnas se ven dentro de una Card (borde hairline + fondo `surface-1`), con las 10 filas visibles **sin scroll de página ni scroll interno** (el `th` sticky sigue funcionando al pasar de 10 filas); y el borde derecho del toggle de tema sigue alineado con el borde derecho del contenido de `main`.
- [x] 11.13 `make test-frontend` en verde y `npm run lint` sin errores nuevos, con los tests de 11.3, 11.4 y 11.9 ya ajustados.

### Corrección del dueño: la Card envuelve la vista entera (dec. 18)

Revierte parcialmente 11.6: la Card deja de ser el marco de la tabla y pasa a ser la raíz de la
plantilla, conteniendo encabezado, filtros, tabla y paginación.

- [x] 11.14 `frontend/src/components/ListPageLayout.tsx`: la `Card` sube a la raíz y reemplaza al `<section>`, con `className="flex flex-col gap-4 p-4 sm:p-5 lg:h-[var(--list-page-height)]"`; el marco de la tabla vuelve a ser `div className="flex-1 min-h-0 overflow-hidden rounded-lg border border-border"` (`rounded-lg`, no `rounded-xl`: radio anidado más chico que el de la Card). **No agregar `py-0 gap-0`**: `cn` es `twMerge(clsx(...))` (`lib/utils.ts`), y tailwind-merge ya elimina el `py-6 gap-6` de `card.tsx:10` al recibir `p-4 sm:p-5` y `gap-4` — agregarlos sería ruido, y omitir el `p-*` sería un bug silencioso (en el orden de utilidades de Tailwind `py-6` gana sobre `p-5` por posición, no por especificidad). Conservar el comentario del `min-h-0` y el `lg:h-[...]` **en la raíz**: con `box-sizing: border-box` el alto incluye el padding, así que el footprint total no cambia (dec. 18).
- [x] 11.15 `frontend/src/pages/Users.tsx`, `STICKY_HEAD_CLASS` (línea ~125): **verificar que `bg-card` sigue siendo el fondo correcto** tras 11.14 — el marco de la tabla es un `div` sin fondo propio, así que detrás del `th` sticky se ve la superficie de la Card y `bg-card` es exacto. Solo hay que cambiarlo si en 11.14 ese `div` termina con un fondo propio distinto (p. ej. `bg-surface-2`), en cuyo caso el `th` tiene que igualarlo: un encabezado sticky cuyo fondo no coincide con el de su contenedor deja ver las filas al scrollear (dec. 7 y 18).
- [x] 11.16 `frontend/src/components/__tests__/ListPageLayout.test.tsx`: agregar/ajustar el caso que afirma que **la raíz de la plantilla es una card** (`data-slot="card"`, el atributo que ya expone `card.tsx:8`) y que el header, la toolbar, los children y el footer quedan **dentro** de ella (dec. 18).
- [x] 11.17 `docs/design/design.md`, "6. List Page": corregir lo que escribió 11.11 — la Card Level 1 contiene **toda** la vista (encabezado, filtros, tabla y pie de paginación), no solo el cuerpo de la tabla; el cuerpo queda como un marco interno con borde hairline y radio menor (dec. 18).
- [x] 11.18 **[Manual, `make dev`]** Re-verificación en 1440x900, dark y light: la vista se ve como **una sola card** que contiene encabezado, filtros, tabla y paginación; las 10 filas siguen entrando **sin scroll de página ni scroll interno**; con más filas que las que entran, solo scrollea el cuerpo de la tabla y el `th` sticky se mantiene. **Re-medir el presupuesto de alto**: el padding de la Card (`p-5` = 40px + 2px de borde) sale de `--list-page-height` (788px en 1440x900); la medición previa dejaba ~123px de aire (fila 10 bottom 722 vs footer top 845) y el contenedor cerraba en `scrollHeight === clientHeight === 621` para ~530px de contenido, así que debería quedar ~80px de margen — confirmarlo, no asumirlo (dec. 18).
- [x] 11.19 `make test-frontend` en verde y `npm run lint` sin errores nuevos, con el test de 11.16 incluido.

### Paridad visual con la tabla de Asistencias (dec. 19)

- [x] 11.20 `frontend/src/index.css`: agregar `--table-head: color-mix(in srgb, var(--surface-2) 40%, var(--surface-1))` a los **dos** bloques de modo (`:root` y `:root[data-theme="light"]`), junto a los `--status-*` de la dec. 12, y mapearlo en `@theme inline` como `--color-table-head` para que exista `bg-table-head`. `color-mix` resuelve a un color **opaco**, que es lo que permite copiar el tono del `bg-surface-2/40` de Asistencias sin violar la restricción de fondo opaco del `th` sticky (dec. 7 y 19.3).
- [x] 11.21 `frontend/src/pages/Users.tsx`, `STICKY_HEAD_CLASS` (líneas ~130-131): agregar **`font-bold`** y cambiar `bg-card` → `bg-table-head`. El `font-bold` (700) es el peso que Asistencias hereda del user-agent para `<th>` y el que `docs/design/design.md` fija para `label-caps`; hace falta explícito porque la clase base de `TableHead` trae `font-medium` (`ui/table.tsx:75`), que pisa el default del UA. No hace falta `!important`: `TableHead` compone con `cn(base, className)` y tailwind-merge resuelve el conflicto de `font-*` a favor del className (dec. 19.2 y 19.3).
- [x] 11.22 `frontend/src/pages/Users.tsx:264`: quitar `showRange={false}` del `<Pagination>` del footer, para que el pie muestre "Mostrando X-Y de Z" a la izquierda y los controles a la derecha, en una fila. El total del header se conserva (redundancia aceptada por el PO). **Si con esto ningún caller pasa `showRange`** (`Attendance.tsx:272` nunca lo pasó), borrar la prop y su condicional de `frontend/src/components/Pagination.tsx`: queda huérfana y el default era `true`, o sea el comportamiento que queda. **No** tocar `getPageRange` en `services/pagination.ts` (dec. 19.1).
- [x] 11.23 `frontend/src/pages/__tests__/Users.test.tsx`: adaptar el caso "hay un unico control de paginacion en la vista" y agregar uno que afirme que el pie muestra el rango — con el mock de 1 usuario que ya usa el archivo, "Mostrando 1-1 de 1"; si se arma un caso con más filas, el rango que corresponda. Sigue habiendo **un solo** control de paginación (dec. 19.1).
- [x] 11.24 `docs/design/design.md`, "5. Data Tables & Quick Forms": documentar el estilo del encabezado de columnas (`label-caps` en peso 700 sobre la banda `--table-head`, opaca por `color-mix` para poder ser sticky) y el pie de la list page (rango a la izquierda, selector de tamaño y navegación a la derecha, en una fila). Anotar como criterio a futuro —no como parte de este change— que las otras cinco tablas escritas a mano del repo (`Attendance.tsx:206`, `Reports.tsx:458` y `:552`, `Dashboard.tsx:615`, `UserRoutine.tsx:299`) adopten `ui/table.tsx` y este mismo estilo cuando se reimplementen (dec. 19.3 y 19.4).
- [x] 11.25 **[Manual, `make dev`]** Re-verificación en 1440x900, dark y light: el encabezado de columnas de Usuarios se ve con el mismo peso y la misma banda de fondo que el de Asistencias; el pie muestra el rango a la izquierda y los controles a la derecha en una sola fila; **las 10 filas siguen entrando sin scroll de página ni interno** (el rango no agrega alto porque comparte fila con los controles a partir de `sm`); y con 14 filas el `th` sticky sigue **opaco** al scrollear — ninguna fila se lee a través del encabezado (dec. 19).
- [x] 11.26 `make test-frontend` en verde y `npm run lint` sin errores nuevos, con el test de 11.23 incluido.

## 12. Hallazgos de verificación

El gate dio **FALLA** (29/30 escenarios, 12 hallazgos — ver `verification.md`). Una task por
hallazgo, en orden de severidad. Ver design dec. 20-23.

- [x] 12.1 **[bloqueante, hallazgo 1]** `frontend/src/lib/utils.ts`: `cn` pasa de `twMerge` a `extendTailwindMerge({ extend: { classGroups: { "font-size": [{ text: [...] }] } } })` declarando los **nueve** tamaños custom que `index.css` define en `@theme inline`: `headline-hero`, `headline-lg`, `headline-md`, `metric-kpi`, `body-lg`, `body-md`, `body-sm`, `label-caps`, `label-code`. Sin esto, tailwind-merge los clasifica como color y `text-muted-foreground` los elimina en silencio (dec. 20). Verificado contra tailwind-merge 3.3.1, la versión instalada.
- [x] 12.2 **[bloqueante, hallazgo 1]** `frontend/src/lib/__tests__/utils.test.ts` (nuevo): (a) `cn("text-label-caps", "text-muted-foreground")` conserva **las dos** clases; (b) `cn` sobre el base de `TableHead` + `STICKY_HEAD_CLASS` conserva `text-label-caps`; (c) **test de drift**: leer `frontend/src/index.css`, extraer los nombres de `--text-<nombre>:` y afirmar que todos están declarados en la config de `cn` — mismo patrón que el drift test de `NAV_ITEMS` ↔ `routeImporters` (dec. 20).
- [x] 12.3 **[mayor, hallazgo 2]** `frontend/src/pages/Users.tsx`, `STICKY_HEAD_CLASS` (~línea 130): agregar `px-4` para alinear los títulos de columna con las celdas `px-4 py-2` (hoy heredan `px-2` de `ui/table.tsx:75` y quedan 8px corridos). **No** tocar el default de `TableHead` en `ui/table.tsx`: la vista ya se apartó del par stock en las celdas y la corrección va en el mismo lugar donde se tomó esa decisión (dec. 21).
- [x] 12.4 **[mayor, hallazgo 3]** `frontend/src/components/Topbar.tsx:105-106`: mover el alto fijo — `<header>` pasa a `lg:h-topbar` y el `div` interior a `h-topbar lg:h-full`. Debajo de `lg` el header crece con el panel del menú y **vuelve a empujar** el contenido en vez de taparlo con un fondo 95% opaco (dec. 4 y 22). Verificar que en `lg` la alineación del borde inferior no se mueve. La línea doble `border-b`/`border-t` se corrige sola al crecer el header: no tocar ninguno de los dos bordes.
- [x] 12.5 **[mayor, hallazgo 4]** `docs/design/design.md:277-279`: borrar las dos afirmaciones revertidas por la dec. 19 — "el total no se repite en el pie" (la línea 248 ya dice lo contrario) y el fondo `bg-card` del encabezado fijo, que ahora es la banda `--table-head`. Documento normativo, no puede tener dos reglas opuestas.
- [x] 12.6 **[menor, hallazgo 5]** `frontend/src/index.css:121-126`: corregir el comentario de `--spacing-topbar`, que dice que la placa de marca del Sidebar comparte la var — falso desde las dec. 16/17. Hoy gobierna solo el Topbar y `--list-page-height`.
- [x] 12.7 **[menor, hallazgo 6]** `frontend/AGENTS.md`: actualizar la sección de Usuarios (ya no hay hero ni card de leyenda: es una list page con leyenda en popover), reemplazar la cita de `max-w-7xl` por `.app-container`, y listar en Tests los 4 archivos nuevos (`ListPageLayout`, `popover`, `navigation`, `pagination`) más el componente `Popover`. El `AGENTS.md` raíz obliga a documentar los tests nuevos en el de la app.
- [x] 12.8 **[menor, hallazgo 7]** `frontend/src/components/ui/__tests__/popover.test.tsx:41-47`: el caso "Enter y Espacio abren" hoy dispara `fireEvent.click` y duplica el caso anterior. **No** agregar `@testing-library/user-event` (no está instalado y no se suma una dependencia por un test): renombrar el caso a lo que verifica y afirmar que el trigger es un `<button>` nativo (`tagName === "BUTTON"`), que es lo que da la activación por teclado (dec. 23).
- [x] 12.9 **[menor, hallazgo 8]** `frontend/src/components/ui/popover.tsx:134`: `aria-controls={open ? contentId : undefined}` — con el popover cerrado ese id no existe en el DOM.
- [x] 12.10 **[menor, hallazgo 9]** `frontend/src/pages/Users.tsx:237`: el `count` produce "1 usuarios". Usar singular/plural condicional.
- [x] 12.11 **[menor, hallazgo 11]** Mover `navItemsForRole(role)` de `frontend/src/components/Topbar.tsx:18-25` a `frontend/src/lib/navigation.ts` y consumirlo desde **los dos**: `Topbar.tsx:103` y `Sidebar.tsx:19-25` (que hoy repite el mismo filtro byte a byte en su `const allowed`). Borrar el comentario de `Topbar.tsx:14-17` que justificaba la duplicación: el hallazgo lo falsea — un item owner-only agregado solo en Sidebar se filtra a coaches en el menú mobile (dec. 23).
- [x] 12.12 **[re-verificación manual, `make dev`]** (a) **Usuarios vs Asistencias lado a lado**, dark y light: comparar en DevTools el `font-size`, el `letter-spacing` y el `font-weight` computados del `th` (deben dar 11px / 0.08em / 700 en las dos vistas) y el `padding-left` del `th` contra el del `td` de su columna (deben coincidir en 16px). (b) **Menú mobile a 390px y a 800px**: abrirlo y confirmar que el contenido de `main` **se desplaza hacia abajo** y queda legible debajo del panel, sin quedar tapado, y que no hay línea doble entre la barra y el panel. Son los dos puntos que el gate no pudo dar por buenos (hallazgos 1, 2 y 3).
- [x] 12.13 `make test-frontend` en verde (incluye el archivo nuevo de 12.2) y `npm run lint` sin errores nuevos.
- [x] 12.14 Actualizar `openspec/changes/redesign-list-page-layout/verification.md`: el escenario "Encabezado de columnas con la tipografía de Asistencias" pasa a PASA y los hallazgos 1-9 y 11 quedan cerrados. Los hallazgos **10 y 12 quedan aceptados**, no corregidos, con la justificación de la dec. 23 — y el 12 (rename "Dashboard" → "Seguimiento" y cambio de orden en el menú mobile) necesita que el PO lo registre en `proposal.md`.
