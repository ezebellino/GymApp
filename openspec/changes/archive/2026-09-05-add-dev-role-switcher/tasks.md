## 1. Backend: configuración y seed

- [x] 1.1 Agregar `ENVIRONMENT: str = "production"` a `Settings` en `backend/app/config.py` (dec. 9 del design). Default deliberadamente inseguro-para-seedear: si nadie declara la variable, el seed se niega. No hace falta tocar `backend/.env.example` ni `backend/.env.docker.example` — los dos ya traen `ENVIRONMENT=development`, que hasta ahora se ignoraba por `extra="ignore"`.
- [x] 1.2 Crear `backend/scripts/seed_dev_users.py` con la constante `DEV_USERS` (única definición del lado backend, dec. 8): tres entradas con `email`, `password`, `first_name`, `last_name`, `role` — `dev.owner@miniespacio.local`, `dev.coach@miniespacio.local`, `dev.member@miniespacio.local`, password `devdev123` en los tres. Seguir el estilo de `backend/scripts/create_owner.py` (mismo bootstrap de `sys.path`, mismos imports de `app.auth`/`app.config`/`app.models`).
- [x] 1.3 En ese archivo, implementar `seed_dev_users(db) -> dict` **sin guardas de entorno ni engine propio** (recibe la sesión): upsert por email — si la fila existe, pisar `first_name`/`last_name`/`password_hash`/`role`/`is_active=True`/`email_verified=True`; si no, crearla. Solo para el rol Miembro: `membership_status = MembershipStatus.active` y `membership_start_date = utcnow()` al crear (dec. 8: sin eso `is_membership_blocking_login` lo rechaza). Dueño y Coach quedan en `MembershipStatus.none`. Un solo `commit()` al final; devolver `{"created": n, "updated": m}`.
- [x] 1.4 Implementar `main()` con las dos guardas **antes de crear el engine** (dec. 9, invariante 6): (a) `settings.ENVIRONMENT.strip().lower()` en `{"development", "local", "test"}`; (b) host de `settings.DATABASE_URL` (`urllib.parse.urlsplit`) en `{"localhost", "127.0.0.1", "db", ""}`. Si alguna falla: imprimir cuál cerró y `sys.exit(1)` (no `return`: un 0 se ve como éxito desde `make`/CI).
- [x] 1.5 Completar `main()`: crear engine/sesión, chequear que la tabla `users` existe con el mismo `SELECT 1 FROM users LIMIT 1` + captura de `ProgrammingError`/`OperationalError` que `create_owner.py` (mensaje que apunte a `alembic upgrade head`), llamar a `seed_dev_users(db)` e imprimir un resumen con los tres emails y la password.

## 2. Backend: tests del seed

- [x] 2.1 Crear `backend/tests/test_dev_seed.py` con un test de idempotencia: llamar `seed_dev_users(db_session)` **dos veces** y afirmar que quedan exactamente 3 usuarios con esos emails, uno por rol, y que el Miembro tiene `membership_status == active` (escenarios "primera corrida crea los tres usuarios" e "correr el seed dos veces no duplica usuarios").
- [x] 2.2 Agregar el test de login end-to-end: tras seedear, `POST /auth/token` devuelve 200 para los tres emails y `GET /auth/me` con ese token devuelve el rol esperado. Es lo que prueba de verdad los gates de `password_hash`/`email_verified`/`is_active`/`membership_status` (dec. 8).
- [x] 2.3 Agregar los dos tests de guarda de entorno (escenario "intento de correr el seed fuera de desarrollo"): con `settings.ENVIRONMENT` monkeypatcheado a `"production"`, `main()` levanta `SystemExit` con código distinto de 0 y no crea usuarios; ídem con `settings.DATABASE_URL` apuntando a un host remoto. Verificar que no se abrió conexión (las guardas corren antes del `create_engine`).

## 3. Frontend: extraer el flujo de login (sin cambiar comportamiento)

- [x] 3.1 Crear `frontend/src/services/auth.ts` con `requestToken({ username, password })` — el `requestTokenWithRetry` actual de `Login.tsx` movido tal cual (timeout 8000, un reintento a los 2 s ante `ECONNABORTED`/timeout) — y `fetchMeWithToken(token)` (`GET /auth/me` con `Authorization` explícito). **Default export de `@/lib/http`**, fetchers puros sin efectos (regla dura de `frontend/AGENTS.md`).
- [x] 3.2 Agregar `signIn({ username, password })` en el mismo archivo: compone los dos anteriores y devuelve `{ accessToken, me }`, con `me: null` si `/auth/me` falló. No toca stores ni navegación.
- [x] 3.3 Crear `frontend/src/hooks/useSignIn.ts`: `useSignIn()` devuelve `signIn(credentials, { resetPreviousSession = false } = {})` que hace, **en este orden** (dec. 4): `services/auth.signIn` → si `resetPreviousSession`, `logout()` → `setSession(token, { name, role, email })` → `setThemeMode(normalizeThemeMode(me?.theme_preference))` → `navigate("/", { replace: true })`. Los errores se propagan (el llamador decide el mensaje).
- [x] 3.4 Reescribir `onSubmit` de `frontend/src/pages/Login.tsx` sobre `useSignIn()`, borrando `requestTokenWithRetry` y la secuencia inline. El mapeo de errores actual (400/401 → toast "Credenciales invalidas", resto → "Error de conexion") se queda en `Login.tsx`. **`frontend/src/pages/__tests__/Login.test.tsx` no se toca**: que siga verde es la prueba de no-regresión.

## 4. Frontend: widget de desarrollo

- [x] 4.1 Crear `frontend/src/components/dev/devUsers.ts` con las tres credenciales (misma tabla que 1.2, dec. 8) y el tipo `DevUserId`. Es la única definición del lado frontend; nada fuera de `components/dev/` la importa.
- [x] 4.2 Crear `frontend/src/components/dev/DevRoleSwitcher.tsx`: card `fixed bottom-4 right-4 z-50` con estado de sesión arriba (leído de `useSessionStore`: Dueño / Coach / Miembro / "sin sesión") y las tres opciones debajo. Tokens semánticos de `docs/design/design.md` (`border-border`, `bg-surface-2/95 backdrop-blur-xl`, `text-foreground`, `text-muted-foreground`), `Button` de `components/ui`, **cero colores crudos**. `data-testid="dev-role-switcher"` (centinela para la verificación de bundle de 5.4).
- [x] 4.3 Implementar el colapsar/expandir (dec. 7): estado en `localStorage["dev_role_switcher_collapsed"]` (`"1"`/`"0"`), lectura con inicializador lazy de `useState` y escritura en el toggle, ambas en `try/catch` (storage bloqueado ⇒ degradar a expandido, nunca romper el render). Colapsado = botón redondo de ~40 px con `aria-expanded={false}` y `aria-label`.
- [x] 4.4 Implementar el handler de cambio de usuario sobre `useSignIn()` con `resetPreviousSession: true`. Estado `busyUserId: DevUserId | null`; el handler arranca con `if (busyUserId) return;` y las **tres** opciones se renderizan `disabled={busyUserId !== null}`, con spinner + `aria-busy` en la elegida y un `aria-live="polite"` que anuncia "Cambiando a …" (dec. 6).
- [x] 4.5 Implementar el mapeo de errores (dec. 5): `err?.response?.status` 400 u 401 → "Usuario de desarrollo no encontrado. Corré `make seed-dev` y volvé a intentar."; cualquier otro → "No se pudo cambiar de usuario. ¿Está levantado el backend?". Se pinta **inline en el widget** con `role="alert"`, no con `toastError`, y se limpia al iniciar un nuevo intento.
- [x] 4.6 Montar el widget en `frontend/src/App.jsx` con el ternario a nivel de módulo (dec. 1): `const DevRoleSwitcher = import.meta.env.DEV ? lazy(() => import("./components/dev/DevRoleSwitcher")) : null;` y, hermano del `<Toaster>` y **fuera** del split `isAuthRoute` (para que también se vea en `/login`), `{DevRoleSwitcher && <Suspense fallback={null}><DevRoleSwitcher /></Suspense>}`. **No** usar `lazy()` incondicional con guarda en el render: el chunk sobrevive al build.

## 5. Frontend: tests y verificación del bundle

- [x] 5.1 Crear `frontend/src/components/dev/__tests__/DevRoleSwitcher.test.tsx` con los casos de UI: (a) se ven las tres opciones etiquetadas Dueño / Coach / Miembro; (b) toggle de colapsar escribe `dev_role_switcher_collapsed`, y con la key sembrada en el `beforeEach` del propio test el widget monta colapsado. Seguir las reglas de `frontend/AGENTS.md`: `renderWithProviders`, siembra de `localStorage` en `beforeEach` (nunca dentro del `it`), `vi.mock("@/lib/http")` con `createApiMock` de `@/test/apiMock`.
- [x] 5.2 Agregar al mismo archivo el caso de error y el de doble submit: con `/auth/token` respondiendo 400, el widget muestra el mensaje que nombra `make seed-dev` **y** `useSessionStore.getState().token` sigue siendo el sembrado antes del click (invariante 3); con `/auth/token` pendiente, las tres opciones quedan `disabled` y hay indicador de carga.
- [x] 5.3 Crear `frontend/src/components/__tests__/App.devSwitcher.test.tsx` con los dos escenarios de `automated-test-suite`: `vi.stubEnv("DEV", true)` + `vi.resetModules()` + `await import("@/App")` + `renderWithProviders(<App />)` → aparece el widget; `vi.stubEnv("DEV", false)` (mismo `resetModules` + import dinámico) → no aparece el widget ni ninguno de los tres labels de usuario. `vi.unstubAllEnvs()` en el `afterEach`. Si `stubEnv` sobre `import.meta.env.DEV` resultara inestable, aplicar el fallback documentado en Riesgos (extraer `components/dev/isDevBuild.ts` y mockearlo con `vi.mock`) en vez de improvisar otro mecanismo.
- [x] 5.4 Verificar la ausencia en producción de verdad: correr `npm run build` en `frontend/` y `grep -r -e "dev-role-switcher" -e "dev.owner@miniespacio.local" -e "devdev123" dist/`. **Cero coincidencias** es la condición de aprobación (invariante 1). Dejar el output pegado en el reporte del change.
- [x] 5.5 Verificar el invariante 2 con `grep -rn "components/dev" frontend/src --include=*.ts --include=*.tsx --include=*.jsx`: la única coincidencia fuera de `components/dev/` debe ser el `import()` de `App.jsx`.

## 6. Makefile y documentación del repo

- [x] 6.1 Agregar el target `seed-dev` al `Makefile` de la raíz con su `## descripción` (lo lista `make help`) y sumarlo a `.PHONY`, con la detección Docker/nativo de la dec. 11 y el `echo` que anuncia qué rama tomó.
- [x] 6.2 Documentar el nuevo target y la tabla de credenciales en `AGENTS.md` (raíz, junto a los comandos del Makefile) y en `backend/AGENTS.md` (junto a los otros scripts de `backend/scripts/`), incluyendo la guarda de entorno y la variable `ENVIRONMENT`.
- [x] 6.3 Documentar en `frontend/AGENTS.md`: el widget de desarrollo (qué es, dónde vive), el **invariante de una sola importación entrante** hacia `components/dev/**`, la razón del ternario a nivel de módulo, la key `dev_role_switcher_collapsed` (y por qué **no** es un cuarto store), y `services/auth.ts` + `hooks/useSignIn.ts` como único camino de login. Sumar los tests nuevos a la lista de "Cobertura actual".

## 7. Flujo de agentes (`.agents/`)

- [x] 7.1 En `.agents/skills/run-app/SKILL.md`, agregar una sección nueva (al final, sin reescribir las existentes — dec. de Riesgos sobre el conflicto con `add-verification-gates-to-opsx-flow`) con: `make seed-dev`, la tabla de los tres usuarios (email / password / rol), y la nota de que el widget flotante abajo a la derecha permite cambiar de rol con un click en modo desarrollo.
- [x] 7.2 En `.agents/skills/role-qa/SKILL.md`, agregar en el paso 4 ("Probá los tres roles") la referencia al widget + `make seed-dev` como la forma prescripta de recorrer los tres roles, en lugar de logins manuales.
- [x] 7.3 En `.agents/skills/role-dev/SKILL.md`, agregar al final de "Reglas duras" que la verificación con `run-app` de un change que toca UI se hace **en los tres roles** usando el widget, no solo en el rol con el que ya se estaba logueado.
- [x] 7.4 Correr `make agents-sync` y después `make agents-check`: no debe reportar drift. No editar nada dentro de `.claude/` ni `.codex/`.

## 8. Cierre

- [x] 8.1 Correr `make test` desde la raíz: backend (incluye `test_dev_seed.py`) y frontend (incluye los tests nuevos y `Login.test.tsx` sin modificar) en verde.
- [x] 8.2 Verificación manual con la skill `run-app`: `make docker-up`, `make seed-dev`, y desde `/login` recorrer Dueño (aterriza en `/dashboard`) → Coach → Miembro (aterriza en `/my-routine`, sin restos del Coach en Sidebar/Topbar); colapsar el widget y recargar (sigue colapsado); y en una base sin seedear, confirmar el mensaje de error que nombra `make seed-dev` con la sesión previa intacta. Dejar screenshots como evidencia.
