# Dockerfile de desarrollo (hot-reload). Para build de producción usá el
# Dockerfile del root de esta carpeta (build + serve estático).
FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
